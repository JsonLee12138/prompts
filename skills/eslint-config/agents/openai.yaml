interface:
  display_name: "ESLint Config"
  short_description: "Set up @antfu/eslint-config + commit hooks"
  default_prompt: "Use eslint-config to set up ESLint for a single project or workspace, with optional commitlint/husky/lint-staged integration."
